# ==========================================================
# SpillWatch — Sentinel-1 SAR Oil Spill Detection Platform
# Production Cloud Dependencies
# ==========================================================

--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# PyTorch Deep Learning Inference (CPU-optimized for Render free tier)
torch>=2.0.0
torchvision>=0.15.0
pillow>=9.5.0

# FastAPI Backend & ASGI Server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# HTTP Clients (sync + async)
requests>=2.31.0
httpx>=0.27.0
aiohttp>=3.9.0

# Data Science & Numerical Processing
numpy>=1.24.0
pandas>=2.0.0

# Database (SQLite built-in, SQLAlchemy ORM, optional PostgreSQL / MySQL)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0; sys_platform != 'win32'
pymysql>=1.1.0

# Geospatial utilities
geopy>=2.4.0
shapely>=2.0.0

# Data validation & serialization
pydantic>=2.0.0

# Logging
python-json-logger>=2.0.7
